{% extends '_layouts/base.njk' %}

{% block page_title %}New index page title{% endblock %}

{% block content %}

<div class="container">
  <div class="row">
    <div class="col-8 offset-md-2">
    <h1>Ahoy Mateys!</h1>
    </div>
  </div>
  <div class="row">
    <div class="col-8 offset-md-2">
      <p>
        These are a few log entries I have written while on my travels over the seas. I have seen many things that I cannot explain, but I'm hoping that there are others out there who will believe me.
      </p>
        {% for entry in data.entries %}
          <h3 class="entry-headline"><a href="{{ entry.url }}">{{ entry.title }}</a></h3>
          <p class="entry-date">{{ entry.date }}</p>
          <hr>
        {% endfor %}
    </div>
  </div>
</div>
{% endblock %}
